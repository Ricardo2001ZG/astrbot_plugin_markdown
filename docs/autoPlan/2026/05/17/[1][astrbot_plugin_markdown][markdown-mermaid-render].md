# markdown-mermaid-render 执行计划

## 任务信息
- 所属项目：astrbot_plugin_markdown
- 任务名称：markdown-mermaid-render
- 日期：2026-05-17 19:50:00 UTC+8
- 今日任务编号：1
- Context：为 AstrBot 插件添加 Mermaid 图表渲染功能，使用 markdown-it-mermaid 插件实现 Markdown 代码块中的 Mermaid 语法自动渲染为 SVG 图表。

## 原始 Prompt

实现 Markdown 文本转 Mermaid 图表并渲染功能，使用 markdown-it-mermaid 插件。

## 目标产物
- `renderer.py` - 更新渲染器，添加 Mermaid 支持
- `src/mermaid_handler.py` - Mermaid 处理模块
- `docs/autoPlan/2026/05/17/[1][astrbot_plugin_markdown][markdown-mermaid-render].md` - 本计划文件

## 关键参考
- markdown-it-mermaid 文档
- Mermaid.js 官方文档：https://mermaid.js.org/
- 项目路径：`/home/lingmiaotech/Projects/LingMiaoTech/LMTechAstrbot/astrbot_plugin_markdown`
- 现有文件：`renderer.py`, `main.py`, `build.mjs`

## 实施步骤

### Step 1: 分析现有项目结构
当前项目已有：
- `renderer.py` - Markdown 渲染器
- `main.py` - 插件入口
- `build.mjs` - 构建脚本
- `src/` - 源码目录

### Step 2: 安装 markdown-it-mermaid 依赖
```bash
npm install markdown-it-mermaid
```
或在 `build.mjs` 中集成 Mermaid 处理。

### Step 3: 识别 Mermaid 代码块
检测 Markdown 中的 Mermaid 代码块：
```markdown
```mermaid
flowchart TD
    A --> B
```
```

### Step 4: 实现 Mermaid 渲染流程
两种方案可选：

**方案 A：前端渲染（推荐）**
- 将 Mermaid 代码块保留为特殊标记
- 前端加载 Mermaid.js 进行渲染
- 适合动态交互图表

**方案 B：后端渲染**
- Python 调用 Node.js 进行预渲染
- 输出静态 SVG 图片
- 适合不需要交互的场景

### Step 5: 创建 Mermaid 处理模块
```python
# src/mermaid_handler.py

import re

class MermaidHandler:
    MERMAID_PATTERN = r'```mermaid\n(.*?)\n```'
    
    def detect_mermaid(self, text: str) -> list:
        """检测并提取 Mermaid 代码块"""
        return re.findall(self.MERMAID_PATTERN, text, re.DOTALL)
    
    def process_mermaid(self, text: str) -> str:
        """处理 Mermaid 代码块"""
        # 根据选择的方案进行处理
        pass
```

### Step 6: 集成到现有渲染流程
修改 `renderer.py`，在渲染流程中添加 Mermaid 处理步骤。

### Step 7: 测试各种 Mermaid 图表类型
支持的图表类型：
- `flowchart` - 流程图
- `sequenceDiagram` - 序列图
- `classDiagram` - 类图
- `stateDiagram` - 状态图
- `gantt` - 甘特图
- `pie` - 饼图

## 验证方式

1. **功能测试**：发送包含 Mermaid 代码块的消息，验证图表正确渲染
2. **多图表测试**：测试各种 Mermaid 图表类型
3. **混合内容测试**：测试 Markdown + Mermaid 混合内容
4. **错误处理**：测试无效 Mermaid 语法的错误提示

## 当前结果
- ✅ 已完成（2026-05-17）
- 实现方案：前端渲染（方案 A）
  - 使用 Mermaid.js CDN 加载
  - 在 render.html 中集成 Mermaid 初始化和渲染
  - 支持主题切换（light/dark）
- 新增文件：
  - `src/handlers/mermaid_handler.py` - Mermaid 代码块检测模块
  - `tests/test_mermaid_handler.py` - 测试套件（11个测试）
- 修改文件：
  - `templates/render.html` - 添加 Mermaid.js CDN 和渲染逻辑
- 测试结果：所有 23 个测试通过

---

*Generated by MorollMiao @ 泠喵科技*
*Plan ID: [1][astrbot_plugin_markdown][markdown-mermaid-render]*